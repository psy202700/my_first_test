# 프로젝트: 점심/저녁 메뉴 추천 웹사이트

## 개요
사용자가 무엇을 먹을지 고민할 때 메뉴를 추천해주는 웹앱.
핵심 기능: 최근에 먹은 메뉴 기록을 고려해서, 너무 자주 먹은 메뉴는 덜 추천하고
다양한 메뉴를 골고루 추천한다.

## 기술 스택
- 순수 HTML/CSS/JavaScript (프레임워크 없이 시작, 별도 빌드 도구 없음)
- 데이터 저장: 브라우저 `localStorage` (서버/DB 없이 개인 기기에 기록 저장)
- 실행: 별도 서버 설치 없이 `index.html`을 브라우저로 열거나,
  `python3 -m http.server` 로 로컬 서버를 띄워 확인

## 폴더 구조
- `index.html` — 메인 화면
- `style.css` — 스타일
- `app.js` — 추천 로직 및 UI 동작
- `data/menus.json` — 메뉴 목록 데이터 (카테고리, 이름 등)

## 핵심 로직 가이드
- 메뉴 추천 시 최근 N일(또는 최근 K회) 안에 먹은 메뉴는 가중치를 낮춰서
  다시 뽑힐 확률을 줄인다 (완전 배제는 아님).
- 먹은 기록은 날짜 + 메뉴 이름으로 저장한다.
- 카테고리(한식/중식/양식/일식/분식 등) 필터링 기능은 추후 확장 여지로 남겨둔다.

## 코드 스타일
- 함수/변수명은 의미가 드러나게 (한국어 주석은 꼭 필요할 때만, 짧게)
- 불필요한 추상화·라이브러리 추가 금지 (바닐라 JS로 충분한 범위에서는 바닐라 JS 유지)

## 확인 방법
브라우저에서 `index.html`을 열거나 `python3 -m http.server 8000` 실행 후
`http://localhost:8000` 접속해서 동작을 확인한다.
